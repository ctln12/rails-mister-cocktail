<div class="home-banner" style="background-image: linear-gradient(rgba(0,0,0,0.3),rgba(0,0,0,0.3)), url(<%= image_url("banner.jpg") %>);">
  <div class="container">
    <h1>Cocktails</h1>
    <p>Share your own amazing drinks to the world!</p>
    <%= link_to "Add a new cocktail", new_cocktail_path, class: "btn btn-primary" %>
  </div>
</div>
<div class="container mt-5">
  <div class="row">
    <% @cocktails.each do |cocktail| %>
      <div class="col-4">
        <%= cl_image_tag cocktail.photo, height: 300, width: 400, crop: :fill, class: "img-fluid" %>
        <p><%= link_to cocktail.name, cocktail_path(cocktail) %></p>
      </div>
    <% end %>
  </div>
</div>
